# Core dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langgraph>=0.4.8
langchain-mcp-adapters>=0.1.10
pydantic>=2.9.0
httpx>=0.26.0
aiohttp>=3.9.0
setuptools>=65.0.0
litellm  # No version constraint - let pip resolve based on all dependencies

# Optional: langchain-litellm (may have compatibility issues with some aiohttp versions)
# If import fails, will fall back to langchain-community ChatLiteLLM
# langchain-litellm>=0.2.0

# MCP client (Model Context Protocol)
# Note: Install from source if not available on PyPI
# mcp>=0.1.0

# Color logging
colorama==0.4.6

# Development and testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.12.1
ruff==0.1.9

