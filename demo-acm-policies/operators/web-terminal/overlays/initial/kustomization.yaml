apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: development-policies

resources:
  - ../../base

# This overlay installs the operator pinned to v1.9.0:
# - remediationAction: enforce (installs the operator)
# - Single version in the versions array (prevents upgrades)
