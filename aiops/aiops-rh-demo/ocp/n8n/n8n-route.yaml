apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: n8n
  name: n8n
  namespace: n8n
spec:
  port:
    targetPort: 5678
  to:
    kind: Service
    name: n8n
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
